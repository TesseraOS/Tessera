{
  "version": 1,
  "updated": "2026-07-04",
  "description": "Ordered verification gates. Run in order; stop at first failure. Code gates went 'active' with the F-001 scaffold. 'e2e' went 'active' with F-011 (the REST API — first user-facing surface). 'a11y' active since F-028 (ADR-0021). 'web-perf' now activates with F-049 (it was slated for F-028 and never activated — drift caught in the 2026-07-04 launch-readiness review). 'e2e-full' (NFR-16) activates with F-048; 'perf' (NFR-4) activates with F-049.",
  "gates": [
    {
      "id": "state",
      "order": 0,
      "status": "active",
      "command": "node scripts/verify-state.mjs",
      "requiredFor": ["always"],
      "description": "Harness integrity gate (hardened 2026-07-04): state files (feature_list.json, effects.json, schema sync), workflow invariants (wip_limit, blockers-done before in_progress/done, plan-before-code for in-flight features), PRD requirement traceability, gates.json validity + CI-mirror (E-005), memory index coverage, governed-doc relative links, and the env-docs guard (every TESSERA_* var read by config/server documented in .env.example)."
    },
    {
      "id": "typecheck",
      "order": 1,
      "status": "active",
      "command": "pnpm -w typecheck",
      "requiredFor": ["code"],
      "description": "TypeScript strict typecheck across the workspace (turbo run typecheck)."
    },
    {
      "id": "lint",
      "order": 2,
      "status": "active",
      "command": "pnpm -w lint",
      "requiredFor": ["code"],
      "description": "ESLint (flat config) incl. package-boundary rule (turbo run lint)."
    },
    {
      "id": "format",
      "order": 3,
      "status": "active",
      "command": "pnpm -w format:check",
      "requiredFor": ["code"],
      "description": "Prettier formatting check (code + root config; docs/harness excluded)."
    },
    {
      "id": "test",
      "order": 4,
      "status": "active",
      "command": "pnpm -w test",
      "requiredFor": ["code"],
      "description": "Unit + integration tests via Vitest (turbo run test), incl. adapter conformance suites."
    },
    {
      "id": "build",
      "order": 5,
      "status": "active",
      "command": "pnpm -w build",
      "requiredFor": ["code"],
      "description": "Turborepo build of all packages and apps."
    },
    {
      "id": "e2e",
      "order": 6,
      "status": "active",
      "command": "pnpm -w test:e2e",
      "requiredFor": ["user-facing"],
      "description": "End-to-end tests across surfaces. api/mcp run HTTP-/SDK-surface e2e (app.inject / in-memory transport; active since F-011). Web (since F-028) runs Playwright against its own dev server, including @axe-core WCAG A/AA assertions. CI installs the Playwright browser before this gate."
    },
    {
      "id": "a11y",
      "order": 7,
      "status": "active",
      "command": "pnpm --filter @tessera/web test:e2e",
      "requiredFor": ["web"],
      "ciStep": false,
      "description": "Accessibility (axe / WCAG 2.1 AA) for apps/web — active since F-028 (ADR-0021). Asserted inside the web Playwright e2e (gate 6); CI's e2e step covers it (ciStep:false — the verify-state ci-mirror guard knows this is deliberate). Listed here for traceability."
    },
    {
      "id": "web-perf",
      "order": 8,
      "status": "planned",
      "command": "pnpm -w test:perf",
      "requiredFor": ["web"],
      "description": "Frontend performance budget (bundle size / Lighthouse CI) for every web app (dashboard + marketing + docs). Planned (ADR-0021); ACTIVATES WITH F-049 (drift fix: originally 'activates with F-028' but was never turned on). Enforces code-split of heavy views, a lean initial bundle, and Core Web Vitals budgets on public surfaces (NFR-17)."
    },
    {
      "id": "e2e-full",
      "order": 9,
      "status": "planned",
      "command": "pnpm -w test:e2e:full",
      "requiredFor": ["release"],
      "description": "Full-stack end-to-end (NFR-16): boot the REAL server (Local profile) → register a fixture repo source → indexed → Playwright drives the real dashboard against that live API (sign-in, sources, search, inspector, memory, audit) AND a real MCP client session exercises the tools. Mimics an actual user + environment. Activates with F-048; exact command finalized there."
    },
    {
      "id": "perf",
      "order": 10,
      "status": "planned",
      "command": "pnpm -w bench",
      "requiredFor": ["release"],
      "description": "Backend performance benchmarks (NFR-4): search p95 < 300 ms local, compile p95 < 2 s at default budget, incremental-ingest latency, and MCP/REST tokens-per-answer (ADR-0036 token-lean requirement) over a versioned fixture corpus; thresholds fail CI on regression. Activates with F-049; exact command finalized there."
    }
  ]
}
