{
  "version": 1,
  "updated": "2026-06-30",
  "description": "Ordered verification gates. Run in order; stop at first failure. Code gates went 'active' with the F-001 scaffold. 'e2e' went 'active' with F-011 (the REST API — first user-facing surface). Web gates ('a11y', 'web-perf') are 'planned' and activate with F-028 (the dashboard); see ADR-0021.",
  "gates": [
    {
      "id": "state",
      "order": 0,
      "status": "active",
      "command": "node scripts/verify-state.mjs",
      "requiredFor": ["always"],
      "description": "Validate harness state files (feature_list.json, effects.json)."
    },
    {
      "id": "typecheck",
      "order": 1,
      "status": "active",
      "command": "pnpm -w typecheck",
      "requiredFor": ["code"],
      "description": "TypeScript strict typecheck across the workspace (turbo run typecheck)."
    },
    {
      "id": "lint",
      "order": 2,
      "status": "active",
      "command": "pnpm -w lint",
      "requiredFor": ["code"],
      "description": "ESLint (flat config) incl. package-boundary rule (turbo run lint)."
    },
    {
      "id": "format",
      "order": 3,
      "status": "active",
      "command": "pnpm -w format:check",
      "requiredFor": ["code"],
      "description": "Prettier formatting check (code + root config; docs/harness excluded)."
    },
    {
      "id": "test",
      "order": 4,
      "status": "active",
      "command": "pnpm -w test",
      "requiredFor": ["code"],
      "description": "Unit + integration tests via Vitest (turbo run test), incl. adapter conformance suites."
    },
    {
      "id": "build",
      "order": 5,
      "status": "active",
      "command": "pnpm -w build",
      "requiredFor": ["code"],
      "description": "Turborepo build of all packages and apps."
    },
    {
      "id": "e2e",
      "order": 6,
      "status": "active",
      "command": "pnpm -w test:e2e",
      "requiredFor": ["user-facing"],
      "description": "End-to-end tests over the MCP/REST surface and dashboard (active since F-011; @tessera/api runs HTTP-surface e2e via app.inject)."
    },
    {
      "id": "a11y",
      "order": 7,
      "status": "planned",
      "command": "pnpm -w test:a11y",
      "requiredFor": ["web"],
      "description": "Accessibility checks (axe / WCAG 2.1 AA) for apps/web. Planned (ADR-0021); activates with F-028 when the dashboard lands — runs within the Playwright e2e flow. Exact command finalized at F-028."
    },
    {
      "id": "web-perf",
      "order": 8,
      "status": "planned",
      "command": "pnpm -w test:perf",
      "requiredFor": ["web"],
      "description": "Frontend performance budget (bundle size / Lighthouse) for apps/web. Planned (ADR-0021); activates with F-028. Enforces code-split of heavy views and a lean initial bundle. Exact command finalized at F-028."
    }
  ]
}
