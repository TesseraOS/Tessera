{
  "version": 1,
  "updated": "2026-06-28",
  "description": "Ordered verification gates. Run in order; stop at first failure. Code gates went 'active' with the F-001 scaffold. 'e2e' activates with the first user-facing feature.",
  "gates": [
    {
      "id": "state",
      "order": 0,
      "status": "active",
      "command": "node scripts/verify-state.mjs",
      "requiredFor": ["always"],
      "description": "Validate harness state files (feature_list.json, effects.json)."
    },
    {
      "id": "typecheck",
      "order": 1,
      "status": "active",
      "command": "pnpm -w typecheck",
      "requiredFor": ["code"],
      "description": "TypeScript strict typecheck across the workspace (turbo run typecheck)."
    },
    {
      "id": "lint",
      "order": 2,
      "status": "active",
      "command": "pnpm -w lint",
      "requiredFor": ["code"],
      "description": "ESLint (flat config) incl. package-boundary rule (turbo run lint)."
    },
    {
      "id": "format",
      "order": 3,
      "status": "active",
      "command": "pnpm -w format:check",
      "requiredFor": ["code"],
      "description": "Prettier formatting check (code + root config; docs/harness excluded)."
    },
    {
      "id": "test",
      "order": 4,
      "status": "active",
      "command": "pnpm -w test",
      "requiredFor": ["code"],
      "description": "Unit + integration tests via Vitest (turbo run test), incl. adapter conformance suites."
    },
    {
      "id": "build",
      "order": 5,
      "status": "active",
      "command": "pnpm -w build",
      "requiredFor": ["code"],
      "description": "Turborepo build of all packages and apps."
    },
    {
      "id": "e2e",
      "order": 6,
      "status": "pending-toolchain",
      "command": "pnpm -w test:e2e",
      "requiredFor": ["user-facing"],
      "description": "End-to-end tests over the MCP/REST surface and dashboard (activates with the first user-facing feature)."
    }
  ]
}
