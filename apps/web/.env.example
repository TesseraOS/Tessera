# Base URL of the Tessera REST API (the running engine — see apps/server).
# The dashboard reads this at build/runtime; copy to `.env.local` to override.
# Defaults to the Local profile address when unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/v1
