import { ComingSoon } from '@/components/coming-soon';

export const metadata = { title: 'Sources' };

export default function SourcesPage() {
  return (
    <ComingSoon
      title="Sources"
      description="Connect and manage filesystem and Git connectors — a later release."
    />
  );
}
