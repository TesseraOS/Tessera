/**
 * Signature illustration layer (DESIGN-SYSTEM §11, ADR-0047). Server-rendered SVG on
 * dashboard tokens, CSS-only motion (frozen under reduced motion), decorative (aria-hidden).
 * Usage budget: empty / error / 404 / onboarding / overview-hero — never data views.
 */
export { Constellation } from './constellation';
export { CompilerAssembly } from './compiler-assembly';
export { SignalField } from './signal-field';
export { PipelineFlow } from './pipeline-flow';
export { MemoryStrata } from './memory-strata';
export { LedgerGate } from './ledger-gate';
export { TimeRiver } from './time-river';
