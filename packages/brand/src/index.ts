/**
 * `@tessera/brand` — the Tessera logo system (BRAND.md §4).
 *
 * One definition of the mark, consumed by every app. See `logo.tsx` for the closed `--brand-*`
 * theming contract and why the fallback is monochrome rather than invisible.
 */
export { Logo, LogoIcon } from './logo.js';
