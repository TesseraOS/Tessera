// GENERATED by scripts/generate.mjs — DO NOT EDIT.
// Source of truth: registry/<name>/SKILL.md. Regenerate with:
//   pnpm --filter @tessera/skills generate

import type { SkillManifest } from '../types.js';

/** Every first-party skill's manifest, sorted by name. Bodies live in `./documents.ts`. */
export const SKILLS = [
  {
    name: "capture-memory",
    description: "Write decisions, lessons, and incidents back to Tessera when work lands, so the next session starts warm instead of re-deriving them. Use after making a non-obvious choice, fixing a surprising bug, or learning something the code does not state.",
    version: "1.0.0",
    category: "workflow",
    headline: "Write down what the code cannot say",
    why: "A reason not recorded is re-derived, or worse, reversed.",
    tools: ["capture_memory", "search"],
    compatibility: "Requires a connected Tessera MCP server (run `tessera init`, or `npx @tessera/cli init`).",
  },
  {
    name: "compile-before-coding",
    description: "Pull a compiled, cited, token-budgeted context package from Tessera instead of reading whole files into the window. Use at the start of any task that needs knowledge of a repository — before opening files, before grepping the tree, before planning a change.",
    version: "1.0.0",
    category: "workflow",
    headline: "Compile context, do not dump files",
    why: "Reading whole files spends the window on text the task never needed.",
    tools: ["compile_context", "search", "explain"],
    compatibility: "Requires a connected Tessera MCP server (run `tessera init`, or `npx @tessera/cli init`).",
  },
  {
    name: "effects-before-editing",
    description: "Ask Tessera what depends on a file, symbol, or module before you change it, and record the couplings the graph cannot infer. Use before editing any shared contract, exported symbol, or configuration value — while the change is still a plan, not a diff.",
    version: "1.0.0",
    category: "workflow",
    headline: "Know the blast radius before the diff",
    why: "A dependent found in review costs more than one found before the edit.",
    tools: ["get_effects", "query_graph", "assert_effect", "search"],
    compatibility: "Requires a connected Tessera MCP server (run `tessera init`, or `npx @tessera/cli init`).",
  },
  {
    name: "project-onboarding",
    description: "Bootstrap a repository into Tessera end to end — register the source, scan it, verify the index is real, and leave an anchor memory behind. Use the first time you work in a repository that Tessera has not indexed yet, or when search and compile come back empty.",
    version: "1.0.0",
    category: "setup",
    headline: "Make a new repository answerable",
    why: "An unindexed repository silently returns nothing, which reads like nothing exists.",
    tools: ["list_sources", "add_source", "scan_source", "get_stats", "search", "compile_context", "capture_memory"],
    compatibility: "Requires a connected Tessera MCP server (run `tessera init`, or `npx @tessera/cli init`).",
  },
] as const satisfies readonly SkillManifest[];

/** The skill names as a non-empty tuple — feeds `z.enum` so the MCP tool schema IS the catalog. */
export const SKILL_NAMES = ["capture-memory", "compile-before-coding", "effects-before-editing", "project-onboarding"] as const;
