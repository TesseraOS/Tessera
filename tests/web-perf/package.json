{
  "name": "@tessera/web-perf",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Frontend performance budgets (F-049; ADR-0021): over-the-wire first-load JS budgets for every web app. CWV/Lighthouse is F-074 — see the header of web-perf.mjs for why it is not gated here.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test:perf": "node web-perf.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22",
    "typescript": "^5"
  }
}
